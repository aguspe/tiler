---
created: 2026-04-23T00:00:00Z
last_edited: 2026-04-23T00:00:00Z
---

# Implementation Tracking: Dashboard Layout

Build site: context/plans/build-site-layout-palette.md
Cavekit: context/kits/cavekit-dashboard-layout.md

## Tasks

- T-101 DONE — Lock panel chrome CSS contract; removed `min-height: 180px` from standalone `.tiler-panel-body` rule in `app/assets/stylesheets/tiler/application.css`. Grid-scoped chrome (flex column panel, `flex:1 1 auto / overflow:hidden / min-height:0 / padding:0.75rem` body) and tile right+bottom borders verified intact. Tests green (140 runs, 0 failures).
- T-104 DONE — Layout PATCH input validation in `app/controllers/tiler/dashboards_controller.rb#layout`. Non-array `items` returns 400 with `{error: "items must be an array"}`. Per-item: items missing any of `id/x/y/w/h` (or with unknown id) are skipped; `applied`/`skipped` counters tracked. Coords clamped: `x` to 0..11, `y >= 0`, `width`/`height` to 1..12. Response always JSON `{applied: N, skipped: M}` with status 200. Existing controller suite (7 runs, 21 assertions) green; system suite (11 runs, 32 assertions) green. AC6 + new branches deferred to T-107.
- T-103 DONE — Added `Tiler.configuration.eager_panel_load` (default `false`) in `lib/tiler/configuration.rb`; wired test env to flip it true via `config.after_initialize` block in `test/dummy/config/environments/test.rb`; `app/views/tiler/dashboards/show.html.erb` turbo_frame_tag now uses `loading: (Tiler.configuration.eager_panel_load ? :eager : :lazy)`. Dropped four reload-polling loops: two `5.times { reload }` workarounds in `test/system/widgets_accessibility_test.rb` (axe a11y + image-alt tests), one `10.times { reload }` workaround in same file (comments active count test), and two force-reload calls in `test/system/dashboard_flow_test.rb` (panels-via-Turbo + add-panel-via-UI tests). All 11 system tests green (32 assertions, 0 failures, 0 errors). Production/dev behavior unchanged — flag defaults false.
- T-102 DONE — Dummy app rebuilds from engine migrations without manual stamping. Added `config.paths["db/migrate"] << File.expand_path("../../../db/migrate", __dir__)` to `test/dummy/config/application.rb` so `bin/rails db:migrate` from `test/dummy` discovers the engine's `db/migrate/*` (resolves to `/Users/augustingottlieb/tiler/db/migrate`). Verified: `db:drop db:create db:migrate` exits 0 with both engine migrations (`20260419000001 CreateTilerTables`, `20260419020000 AddGridLayoutToTilerPanels`) showing `up` in `db:migrate:status`. Smoke runner returns `Tiler.widgets.types` (14 widgets: bar_chart,clock,comments,iframe,image,line_chart,list,meter,metric,number_with_delta,pie_chart,status_grid,table,text) and `Tiler::Dashboard.create!` persisted=true. Full suite green (140 runs / 287 assertions / 0 failures). `test/dummy/db/schema.rb` left untouched (still version 2026_04_19_020000 — used by `db:test:prepare`). No engine migrations mirrored into `test/dummy/db/migrate/`. `.github/workflows/ci.yml` already uses standard `bin/rails db:test:prepare test` + `bin/rails test:system` — no `INSERT INTO schema_migrations` or hand-stamping found, so no CI changes needed (R5 AC4 satisfied as-is).
- T-106 DONE — Added `text` widget to opt-in scroll allowlist in `app/assets/stylesheets/tiler/application.css`. New rule `.tiler-grid-stack .tiler-text { max-height: 100%; overflow: auto; }` placed adjacent to existing list/status_grid/table allowlist rules (line 52, before `.tiler-grid-stack .tiler-metric`). Kept `text` in its own rule (no `margin: 0`) so paragraph spacing inside text widget preserved. Closes R2 finding F-003 (multi-paragraph text panels now scroll instead of clipping). CSS-only change — full suite green (140 runs / 287 assertions / 0 failures).
- T-107 DONE — Controller test coverage for layout PATCH R3 branches. Added `setup do @dash = create_dashboard end` and 6 new tests to `test/controllers/tiler/dashboards_controller_test.rb`: (1) clamps negative x to 0, (2) clamps x>11 to 11, (3) clamps negative y to 0, (4) returns 400 + JSON `{error:}` for non-array items, (5) skips items with missing id and counts skipped (asserts `applied:1, skipped:1`), (6) response shape is `{applied:Integer, skipped:Integer}`. File now has 13 tests total (7 existing + 6 new). Verified: `bundle exec rails test test/controllers/tiler/dashboards_controller_test.rb` → 13 runs, 35 assertions, 0 failures, 0 errors, 0 skips. No controller code touched (T-104 already shipped impl).
- T-115 DONE — System test for R2 AC4 (computed-style scroll policy per widget class). New file `test/system/widget_scroll_policy_test.rb` seeds one panel per registered widget type at w=3 h=2 across the dashboard, then visits show and uses `page.evaluate_script` to walk every `.grid-stack-item` → `.tiler-panel-body`, detect the inner widget root via `[class*="tiler-"]` regex (handles hyphen+underscore class forms; e.g. `tiler-status-grid` → key `status_grid`), and assert (a) every panel body resolves `getComputedStyle(...).overflow === 'hidden'`, (b) each allowlisted widget (`list/table/status_grid/text`) has a child `.tiler-<class>` element whose computed `overflow` is `auto` or `scroll`. Setup hydrates configs that need `aggregation`/`group_by`/`bucket`/`limit` so partials emit their root elements (rather than the muted-placeholder fallback). Verified: `bundle exec rails test test/system/widget_scroll_policy_test.rb` → 1 run, 4 assertions, 0 failures, 0 errors, 0 skips. CSS NOT modified — passes against the existing R1 grid-scoped clip rule + R2 allowlist (lines 47, 49–52 of `app/assets/stylesheets/tiler/application.css`). DO NOT COMMIT per task constraint.
- T-114 DONE — No-overflow regression smoke system test added at `test/system/widget_overflow_test.rb`. Seeds one panel of every registered widget type (14 total) on a single dashboard at tight `width:3, height:2` tiles, arranged 4 per row across the 12-col grid. With `Tiler.configuration.eager_panel_load = true` (T-103) turbo-frames render synchronously — no reload polling. After tiles render, JS `evaluate_script` walks every `.grid-stack-item`, classifies the widget by the `tiler-<class>` root inside `.tiler-panel-body`, skips the R2 scroll allowlist (`list`, `table`, `status_grid`, `text`), and asserts `scrollHeight <= clientHeight + 1` for the rest. Seeded 10 records with `status/duration/value` keys so data-backed widgets render meaningful content. Verified: `bundle exec rails test test/system/widget_overflow_test.rb` → 1 run, 4 assertions, 0 failures, 0 errors, 0 skips. NO widget needed allowlist exception — every non-allowlisted widget (clock, metric, number_with_delta, meter, image, comments, iframe, line_chart, bar_chart, pie_chart) clipped within its 3x2 tile; R1/R2 CSS contracts hold. (First run hit a transient `SQLite3::BusyException`/`database is locked` from a leftover dummy puma server on :3131 — second run green; infra issue, not a test/code bug.) DO NOT COMMIT per task constraint.
