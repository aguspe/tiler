---
created: 2026-04-23T00:00:00Z
last_edited: 2026-04-23T00:00:00Z
---

# Implementation Tracking: Widget Palette Drag-Drop

Build site: context/plans/build-site-layout-palette.md
Cavekit: context/kits/cavekit-widget-palette-drag-drop.md

## Tasks

- T-105 DONE — Added `default_config` and `default_size` class attrs to `Tiler::Widget` base in `lib/tiler/widget.rb`. Defaults set to `{}` and `{w: 6, h: 2}`. Sanity check via `bundle exec rails runner` from `test/dummy` returned `{}` and `{w: 6, h: 2}`. `bundle exec rails test test/lib/` failed in env setup (pre-existing migration conflict, scope of T-102) — not caused by this change.
- T-108 DONE — Per-widget `default_config` and `default_size` overrides applied to all 14 built-in widgets in `lib/tiler/widgets/*.rb`. Note: T-105 used `attr_accessor` (not `class_attribute`), so subclasses do not inherit the base `{}` / `{w:6,h:2}` automatically — every widget explicitly sets both. New parameterized test at `test/lib/tiler/widgets/defaults_test.rb` (14 widgets x 3 cases = 42 runs, all green). Full `test/lib/` suite: 133 runs, 263 assertions, 0 failures (was 91 runs pre-T-108, +42 new). Defaults respect R6: image and iframe omit `url` (no scheme violation), meter omits `max` (forces user config — surfaces "Configure max" placeholder), comments omits `quote_column`, and all `aggregation` values are in `%w[avg sum max min last count]`.
- T-110 DONE — Palette UI markup, CSS, and edit-mode visibility toggle wired in `app/views/tiler/dashboards/show.html.erb` and `app/assets/stylesheets/tiler/application.css`.
  - Added `id="tiler-grid-stack-#{@dashboard.id}"` to `.grid-stack` div (resolves T-111 turbo_stream append target).
  - Wrapped grid + new `<aside class="tiler-widget-palette" data-tiler-palette>` in `.tiler-dashboard-shell` flex container inside the non-empty branch. Palette iterates `Tiler.widgets.each` and emits one `<li class="tiler-widget-palette-item" data-tiler-palette-widget data-widget-type=<type> data-default-w data-default-h data-default-config draggable="true" tabindex="0">` per registered widget with `klass.label`.
  - JS `setEditing(on)` mirrors the `tiler-editing` toggle onto `.tiler-dashboard-shell` as `tiler-editing-mode` class so palette CSS visibility hooks fire.
  - CSS appended at end of stylesheet: `.tiler-dashboard-shell` flex container, `.tiler-dashboard-shell .tiler-grid-stack { flex: 1 1 auto; min-width: 0; }`, palette hidden by default with `display:none`, shown via `.tiler-dashboard-shell.tiler-editing-mode .tiler-widget-palette { display: block; }`. Tile styling (cursor:grab, hover/active/focus states), 200px sidebar, `align-self: flex-start`, `max-height: 80vh; overflow: auto`.
  - R1 ACs satisfied: AC1 (palette visible when edit-mode class on shell), AC2 (display:none when off — no layout shift since palette is `flex: 0 0 200px` only when shown), AC3 (one tile per registered widget via `Tiler.widgets.each`), AC4 (each tile has `klass.label` text + `draggable="true"`). AC5 (system test) deferred to T-116 per plan.
  - Verified: `bundle exec rails runner 'puts Tiler::Dashboard.find_by(slug: "demo").panels.count'` from `test/dummy` returns 11 (demo dashboard intact). System tests blocked by pre-existing T-102 schema/migration conflict (`Duplicate migration 20260419000001`), not introduced by T-110 — same env issue documented under T-105.
  - Constraints respected: no JS drop handler / `gridstack.acceptWidgets` (deferred to T-112). No widget class or registry changes.
- T-111 DONE — `panels#create` now multi-format (html / json / turbo_stream) for the drop flow.
  - Strong params already permitted `widget_type, title, width, height, x, y, config, tiler_data_source_id` — no change needed (verified).
  - Coord validation: model already enforces R3 contract (`x: 0..11`, `y >= 0`, `width/height: 1..12`); drop coords flow through `panel.save` and are rejected if out of range. Same rule set as `dashboards#layout` PATCH (T-104).
  - JSON success branch: 201 + `{id, widget_type, title, x, y, width, height, config, preview_url}` via `panel_json` helper.
  - JSON failure branch: 422 + `{errors: [...full_messages...]}`.
  - Turbo Stream success branch: renders new `app/views/tiler/panels/create.turbo_stream.erb`, which appends a `.grid-stack-item` (with `gs-id/x/y/w/h`) wrapping the panel's lazy turbo-frame preview into target id `tiler-grid-stack-#{@dashboard.id}`.
  - Turbo Stream failure branch: returns 422 JSON errors (defer rich form-error stream until T-110/T-112 wire flash UI).
  - DEFERRAL: the turbo_stream view targets DOM id `tiler-grid-stack-<dashboard.id>`, which T-110 must add to the `.grid-stack` div (currently the div only carries `data-tiler-dashboard-id`). View is ready but the append target won't resolve until T-110 lands. Documented inline in the .erb header.
  - Verified: existing `bundle exec rails test test/controllers/tiler/panels_controller_test.rb` — 6 runs, 15 assertions, 0 failures. Manual `Rack::Test` POSTs confirmed JSON 201 + 422 (`Widget type is not a registered widget` for forged type) and Turbo Stream `<turbo-stream action="append" target="tiler-grid-stack-1">` payload with the expected `<turbo-frame>` skeleton.
- T-109 DONE — Appended 7 R6 security/validation tests to `test/lib/tiler/widgets/defaults_test.rb`: image URL scheme allowlist (https?:// only, refute javascript:/data:/file:); meter `aggregation` enum allowlist (`%w[avg sum max min last]`); meter omits `max` (R9 required-key placeholder); image omits `url` (R6/R9 placeholder); comments omits `quote_column` (R9 placeholder); iframe omits `url` (R6/R9 placeholder); cross-cutting check that every registered widget's default `aggregation` (when present) is in `%w[avg sum max min last count]`. Test count: 42 (T-108) + 7 (T-109) = 49 runs, 86 assertions, 0 failures, 0 errors. Ran via `bundle exec ruby -Itest test/lib/tiler/widgets/defaults_test.rb` because `bundle exec rails test` still hits the duplicate-migration env error (T-102 scope). No widget class files modified — defaults from T-108 already satisfy every assertion.
- T-113 DONE — Appended 3 R5 controller tests to `test/controllers/tiler/panels_controller_test.rb` covering the forged/blank/registered widget_type cases. Used `dashboard_panels_path(dash.slug)` to mirror the existing `POST create` test (slug-keyed nested route, not raw record). Tests posted with `as: :json` and asserted `:unprocessable_entity` (forged + blank) and `:created` (image happy-path). Validation enforcement comes from the existing model-level `validate :widget_type_registered` in `app/models/tiler/panel.rb` plus `validates :widget_type, presence: true` — controller untouched per task constraints. Controller's `respond_to` (T-111) maps `format.json` failure to 422 + errors and success to 201 + `panel_json`. Verification: `bundle exec rails test test/controllers/tiler/panels_controller_test.rb` reports 9 runs, 24 assertions, 0 failures, 0 errors, 0 skips (6 prior + 3 new).
- T-116 DONE — System test `test/system/widget_palette_test.rb` covering R1 ACs (palette hidden by default / visible in edit mode, one tile per registered widget, label + default-w/h/config data attrs, keyboard-reachable tabindex). 4 runs, 90 assertions, 0 failures, 0 errors via `bundle exec rails test test/system/widget_palette_test.rb` from `/Users/augustingottlieb/tiler`. No palette markup, CSS, or JS handler modified — pure test addition.
- T-112 DONE — JS drop handler wired in `app/views/tiler/dashboards/show.html.erb` IIFE. Added `acceptWidgets: true` to `GridStack.init` options. Called `GridStack.setupDragIn('.tiler-widget-palette-item', { appendTo: 'body', helper: 'clone' })` (gridstack v10.3.1 supports this v8+ API per layout `<script src=".../gridstack-all.js">`). Registered `grid.on('dropped', function(_event, _previousNode, newNode) { ... })`. Handler reads `data-widget-type` and `data-default-config` from the dropped helper element, derives `title` from the `.tiler-widget-palette-label` text, calls `grid.removeWidget(src, false, false)` to clear gridstack's placeholder (turbo_stream response from T-111 appends the real tile), then `fetch(dashboard_panels_path(@dashboard), { method:'POST', headers:{'X-CSRF-Token': el.dataset.tilerCsrf, 'Accept':'text/vnd.turbo-stream.html'}, body: FormData })` posting `panel[widget_type|title|x|y|width|height|config]`. On non-2xx, logs warning (R3 AC3 — placeholder already removed). On 2xx, `Turbo.renderStreamMessage(html)` renders the Turbo Stream from T-111, then `setTimeout(100ms)` walks `.grid-stack-item:not(.gs-w)` and calls `grid.makeWidget(item)` on any element gridstack hasn't hydrated yet (so subsequent move/resize PATCH layout fires). On cancel-mid-drag (release outside `.grid-stack`), gridstack never fires `dropped` — no XHR (R4 satisfied natively).
  - URL embed: `<%= raw dashboard_panels_path(@dashboard).to_json %>` — JSON-quoted to safely interpolate into JS string literal.
  - Verified: ran `bundle exec rails test test/system/` from `/Users/augustingottlieb/tiler` → 11 runs, 31 assertions, 1 failure (`test_can_add_a_new_panel_via_UI`). Confirmed via `git stash push -- app/views/tiler/dashboards/show.html.erb` + re-run that the failure pre-exists without T-112's changes (form-based create flow, unrelated to drop handler). All other system tests pass — drop handler is purely additive.
  - Constraints respected: did NOT touch `panels_controller.rb` (T-111 territory), did NOT modify palette aside markup (T-110 territory), did NOT write a system test (T-116/T-117/T-118 own that). No commit per dispatch instruction.
  - Gridstack API surprises: none. v10.3.1 `setupDragIn` and `dropped` event signature `(event, previousNode, newNode)` confirmed against the layout's CDN script tag. The `gs-w` selector exclusion in the makeWidget walk is defensive — gridstack stamps the `gs-w` data marker on hydrated items.
- T-117 DONE — Cancel-mid-drag system test added at `test/system/widget_palette_cancel_test.rb`. Two test cases cover R4 AC1 (drop outside grid creates no panel; no `.grid-stack-placeholder` ghost remains) and R4 AC2 (no POST fires when `dropped` event is never triggered — verified via stable `@dash.panels.count` after edit-mode toggle round-trip). Setup seeds one `clock` panel so dashboard show renders the `.grid-stack` + `.tiler-widget-palette` (the empty-dashboard branch suppresses both). Strategy mirrors T-112's note that gridstack's `dropped` event only fires on successful drop into the grid — releasing outside (or never releasing) yields no event, hence no XHR. Verified: `bundle exec rails test test/system/widget_palette_cancel_test.rb` from `/Users/augustingottlieb/tiler` → 2 runs, 5 assertions, 0 failures, 0 errors. Constraints respected: no palette markup / CSS / JS / controller modifications. No commit per dispatch instruction.
- T-118 DONE — Parameterized system test `test/system/widget_palette_drop_test.rb` (R7 AC1+AC2). Generates one `test_drop_<type>_creates_panel_and_renders_preview` method per `Tiler.widgets.types` value (14 widgets → 14 runs). Each test enters edit mode, asserts the palette tile for that widget exists, then uses `page.evaluate_async_script` to POST `panel[widget_type|title|x|y|width|height|config]` to `dashboard_panels_path` (mirrors the JS drop handler in `show.html.erb` rather than racing pixel-drag in headless Chrome — same pattern as `dashboard_flow_test.rb` for move/resize). Asserts a new `Tiler::Panel` row exists with the widget_type. Visits the preview turbo-frame URL in a sub-session and refutes Rails 5xx markers (`ActionView::Template::Error`, "We're sorry", dev exception page) — selenium driver doesn't expose `status_code`, so HTML sniff is the only signal.
  - Used `evaluate_async_script` (not `execute_script`) so the test waits for the fetch promise to resolve before asserting on the DB; otherwise the test races the puma roundtrip.
  - Used `Capybara.using_session("preview-#{widget_type}")` so the preview-page visit doesn't clobber the dashboard session for subsequent tests.
  - VERIFICATION: `bundle exec rails test test/system/widget_palette_drop_test.rb` from `/Users/augustingottlieb/tiler` → 14 runs, 126 assertions, 0 failures, **1 error**.
  - **WIDGET FAILING PREVIEW**: `number_with_delta` raises `NoMethodError: undefined method 'data_records' for nil` from `lib/tiler/widgets/number_with_delta.rb:15` (`Tiler::Widgets::NumberWithDeltaQuery#call`), via `Panel#data` → `Widget#data` → `query_class.new(panel, config).call`. Root cause: `number_with_delta`'s `default_config` from T-108 lets the panel be created with no `tiler_data_source_id` bound, but the query class dereferences `panel.data_source.data_records` without nil-guarding the data_source (the way `clock` is protected by `query_class.nil?` in `Widget#data`). This is a **T-108 bug** (per kit R2 AC2: "each must declare a default_config that is a valid Hash and renders without raising via `panel.data` and the partial"). Per dispatch instructions, did NOT modify the widget — fix is out of T-118 scope. The other 13 widgets (`clock, text, iframe, image, comments, metric, meter, list, table, pie_chart, bar_chart, line_chart, status_grid`) all persist + render their preview without server error.
  - Constraints respected: no widget class, partial, controller, palette markup, or JS handler modified. No commit per dispatch instruction.
- T-119 DONE — System test `test/system/widget_palette_multi_test.rb` covering R7 AC3 (multi-drop persistence), AC4 (drop-then-move PATCH layout fires), and AC5 cross-reference (cancel coverage already in T-117). Two test cases:
  1. **Multi-drop**: drops `clock` then `text` via back-to-back `evaluate_async_script` POSTs to `panels#create`; asserts `@dash.panels.count` increased by 2 and both widget_types persist.
  2. **Drop-then-move**: drops `text` at (0,4) with full hydration (renders the turbo_stream response via `Turbo.renderStreamMessage`, then calls `grid.makeWidget` on un-hydrated `.grid-stack-item`s — mirrors the production handler in `show.html.erb`). Then uses `grid.update(widget.el, { x:6, y:5 })` to fire the change event; polls DB until PATCH layout persists `x=6, y=5`.
  - Hydration was the only non-obvious bit: T-118's pattern of fetching directly does NOT render the stream into DOM, so the dropped panel's `[gs-id]` element doesn't exist for gridstack to move. Added optional `hydrate:` flag on the `drop_widget` helper that renders the turbo_stream + walks `.grid-stack-item:not(gridstackNode)` calling `makeWidget`, exactly as `show.html.erb`'s production handler does.
  - VERIFICATION: `bundle exec rails test test/system/widget_palette_multi_test.rb` from `/Users/augustingottlieb/tiler` → 2 runs, 13 assertions, 0 failures, 0 errors, 0 skips.
  - Constraints respected: no partials, controllers, or palette markup touched. No commit per dispatch instruction.
